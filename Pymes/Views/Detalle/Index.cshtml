@model IEnumerable<DataLayer.DetalleVenta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Producto.NombreProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venta.Direccion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.NombreProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venta.Direccion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetalleVenta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetalleVenta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetalleVenta })
        </td>
    </tr>
}

</table>
