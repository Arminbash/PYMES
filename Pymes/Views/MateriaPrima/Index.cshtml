@model IEnumerable<DataLayer.MateriaPrima>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreMateriaPrima)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrecioXUnidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proveedor.Nombres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreMateriaPrima)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrecioXUnidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedor.Nombres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdMatriaPrima }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdMatriaPrima }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdMatriaPrima })
        </td>
    </tr>
}

</table>
